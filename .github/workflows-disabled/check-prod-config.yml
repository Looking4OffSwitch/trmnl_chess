name: Check Prod Config

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check-prod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run prod config check
        run: make check-prod
